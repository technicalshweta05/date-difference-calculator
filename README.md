# date-difference-calculator
This Date Difference Calculator lets users select two datetimes, computes the difference in milliseconds, and converts it into total days and remaining hours. Results are logged to the console, providing a quick and accurate measure of time intervals between events.
